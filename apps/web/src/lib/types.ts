export type AppRole = "customer" | "property_manager" | "staff" | "admin";

export type BookingStatus =
  | "requested"
  | "scheduled"
  | "in_progress"
  | "completed"
  | "cancelled";

export type SubscriptionStatus = "active" | "paused" | "cancelled";
